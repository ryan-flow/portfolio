# 🔧 作品集修正计划

> 基于全站评审 + heritage + ink 三份深度分析

---

## P0 · 数据纠错（今天）

### 1. ink-studio 项目名 + 描述 + 技术栈全部纠正

| 维度 | 当前（错误） | 修正 |
|------|-------------|------|
| 项目名 | 前端设计实验室 | **墨韵 · 数字水墨** |
| 描述 | 前端交互与视觉... | **SVG水墨场景 + Canvas墨迹扩散 + Web Audio古琴音景** |
| 技术栈 | HTML, CSS, JS | **SVG, Canvas 2D, Web Audio API, CSS Animation** |
| THINKING | 零框架依赖... | **零框架零依赖，单文件实现从SVG场景引擎到Canvas题诗作画** |

### 2. heritage 技术栈标签补全

| 当前 | 补全后 |
|------|--------|
| `Python, FastAPI, SQLAlchemy, 微信小程序` | **+ Neo4j, React, TypeScript, Vite, Crawler** |

### 3. 四项目加入量化指标

```
heritage: 16张表 / 12个API端点 / 21个路由页面 / 200+知识节点 / 8个CRS模块
arknights: 首屏1.2s / 4大组件模块 / GitHub Pages部署
refine: 完整数据处理流水线(5步骤) / refineyourself.asia独立域名
ink: 5种主题配色 / 单文件自包含 / 零依赖
```

---

## P1 · 卡片增强（今天/明天）

### 4. 桌面卡片宽度 640→720px

适配 3:4 比例，视觉填满度从 ~60% 提升到 ~68%

### 5. 统一「AI应用落地」叙事线

四个项目按技术复杂度排序：
```
墨韵 → 人格测试 → 人格克隆 → CRS平台
 基础    工程       AI工程     全栈系统
```

每个 THINKING 开头加一句项目定位标签：
- 「创意实验」- 墨韵
- 「工程实践」- 人格测试
- 「LLM 探索」- 人格克隆
- 「毕设/全栈系统」- CRS 平台

### 6. THINKING 文案重构

#### heritage
```
THINKING: [定位: 全栈系统 · 毕设]
基于ASK-REC引擎的多轮对话推荐，置信度三维状态机（显式偏好+隐式行为+交互数据）保障推荐质量。
五级回退策略（本地KB→豆包LLM→网络搜索）实现100%回答覆盖。
Neo4j存储200+非遗知识节点，推荐解释可溯源。
独立负责数据库建模(16表)、API设计(12端点)、双前端(小程序+React Web，21页面)。
```

#### arknights
```
THINKING: [定位: 工程实践]
状态机驱动的自定义对话引擎，题目分支逻辑与计分解耦，组件化拆分为题目渲染/选项交互/分数聚合/结果展示四个模块。
首屏加载1.2s，Tailwind CSS 移动端适配，GitHub Pages 部署，支持一键分享结果卡片。
```

#### refine
```
THINKING: [定位: LLM探索]
从原始聊天数据到可交互AI分身的完整流水线：清洗→分词→特征向量化→LLM微调→推断。
通过Prompt层约束保证输出风格一致性（语气/用词/情感倾向），验证了"少量数据可复刻人格"的假设。
独立域名部署 refineyourself.asia。
```

#### ink
```
THINKING: [定位: 创意实验]
零框架零依赖，单个index.html自包含完整数字水墨体验。
技术组合：SVG场景引擎(山脉竹林仙鹤锦鲤) + Canvas 2D题诗作画(一键导出) + Web Audio古琴泛音音景。
五种主题配色(墨韵青花金碧朱砂雪景)，书法风格留言墙。
验证了"纯前端可实现完整艺术交互体验"的技术上限。
```

### 7. 桌面箭头按钮优化

- disabled 态加 `opacity: 0.15`（当前几乎不可见）
- hover 态加微动画

---

## P2 · 全局优化（本周）

### 8. Hero 区加身份标签

在「王子轩」下方加一行：
```
AI Agent 工程师 · 数据科学 2026 届
```

### 9. 压缩页面空白

- `#projects` 往上移（`py: md:8 → md:6`）
- `#about` 往上移
- 各 section 间空白从 4rem 压到 2.5rem

### 10. Skills 区调整

- 进度条 → 标签云
- 按类别分组：框架(React/FastAPI) / 语言(Python/TS) / 工具(Neo4j/Vite) / 平台(微信小程序/Vercel)

---

## P3 · 后续迭代

### 11. 增加「数字说话」板块

在 About 下方或 Footer 上方：
```
4 个项目 · 16 张数据表 · 200+ 知识节点 · 21 个页面 · 12 个 API 端点
```

### 12. 可考虑的新项目方向

当前 ink-studio 与其他三个 AI 项目风格差异大。未来可添加：
- 一个 Claude Code MCP 工具项目（更贴近 AI Agent 定位）
- 或一个 LangChain/LlamaIndex 实践项目

---

## 执行顺序

```
P0 数据纠错（5分钟）
  → P1 卡片+文案（15分钟）
    → 桌面验证 + 手机验证
      → Git push
        → P2 全局优化（按需）
```

---

**下一步**：从 P0 #1 开始，逐项实施。
